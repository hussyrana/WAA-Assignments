spring:
  application:
    name: demo
  datasource:
    password: Jason121
    url: jdbc:postgresql://localhost:5432/Assignment1-DataBase
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
    generate-ddl: true
  sql:
    init:
      mode: always
server:
  port: 8080